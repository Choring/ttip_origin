# 🚀 2026년 4월 15일 진행 목표 (Todo List)

어제까지 커뮤니티 게임화 시스템(포인트/티어), 북마크 기능, 관리자 대시보드 통계 기초 작업을 마무리한 상태입니다.
오늘은 남은 미완성 기능을 완성하고, UX 세부 다듬기 및 공유/SEO 강화에 집중합니다.

---

## 📌 오늘 진행할 주요 개발 작업

### 1. ✅ 포인트 적립 시스템 실제 연동 확인 및 UI 반영
- [x] **헤더/프로필 UI**: 로그인 유저의 현재 포인트(current_points)와 티어 아이콘/등급명을 헤더 또는 사이드바에 표시.
- [x] **포인트 획득 토스트**: 게시글 작성, 댓글, 좋아요(띱) 수신 시 `point_gain` 세션 플래시를 감지해 화면에 황금빛 전용 토스트 표시.
- [x] **게시글 작성 포인트 연동**: `PostController::store()`에 +50P 적립 로직 추가 (기존 댓글/띱은 이미 연동됨 확인).

### 2. ✅ 관리자 게시글 편집 페이지 강화 (Admin > Post > Edit.vue)
- [x] **리치 텍스트 에디터**: TiptapEditor가 이미 연결되어 있었음 확인. 섹션별 레이아웃 구조화 및 에러 메시지 표시 추가.
- [x] **관리자 공지 타입 선택**: `type` 라디오 카드(일반/공지/상단고정) + `is_pinned` 체크박스 UI 추가, 백엔드 validation 연동.
- [x] **Admin Post Index 개선**: 목록 상태 컬럼에 📌상단고정 / 📢공지 뱃지 추가.

### 3. ✅ 관리자 공지(Notice) 등록 및 노출 로직 완성
- [x] **전체 공지(all notice) 등록**: 공지 카테고리를 `all`(전체공지)로 제한 완료. `store()` validation에서 공지 전용 카테고리 외 선택 시 에러 반환.
- [x] **홈 피드 중복 방지**: `scopePinnedNotice()` 신규 추가 → `notice()` + `pinned()` 중복 체인 정리. 일반 피드에서 `type IN('notice','pinned')` 전체 제외하여 공지가 피드에 섞이지 않도록 수정.
- [x] **카테고리 탭 전환 시 공지 표시**: `Home.vue`에서 `currentCategory === 'all'` 조건 제거 → 어떤 카테고리 탭에서도 상단 고정 공지 항상 표시.

### 4. ✅ 게시글 공유 기능 전면 배포 (Universal Share)
- [x] **ShareButtons.vue 컴포넌트 신규 생성**: 링크 복사(Clipboard API + 복사됨 피드백) / X(트위터) 공유 / 카카오톡 공유 / 모바일 네이티브 공유(Web Share API) 4종 버튼.
- [x] **Post/Show.vue 적용**: Like Action Bar 아래 `이 글 공유하기` 섹션에 ShareButtons 삽입. 기존 단순 공유 버튼 제거.
- [x] **OpenGraph/Twitter Card 완성**: `og:type`, `og:site_name`, `og:url`, `og:image`(썸네일 있을 때만), `twitter:card`(이미지 유무 따라 summary/summary_large_image) 태그 추가.

### 5. ✅ 방문자 통계 품질 개선 (Admin Dashboard)
- [x] **주간/월간 탭 전환**: 헤더 우측 `최근 7일 / 최근 30일` 탭 버튼 추가. `activeTab`에 따라 chart7/chart30 전환. `:key` 바인딩으로 차트 강제 리렌더링.
- [x] **방문자 카운팅 확인**: `TrackVisits` 미들웨어가 IP 기반 일별 중복 방지 카운팅 정상 구현. 데이터 없는 날도 0으로 채워 차트 누락 방지.
- [x] **차트 시각 개선**: 라인차트 fill 효과, 바차트 borderRadius, 기간 합계/일평균 요약 카드, 하단 배너에 총 회원 추가.

### 6. ✅ SEO 및 서비스 안정화 마무리
- [x] **Sitemap 개선**: `SEOController`에서 공지 카테고리(`slug=all`) 제외, 공지/광고 게시글 제외(일반만), 최근 500개 제한. `image:image` 네임스페이스 추가로 썸네일 있는 게시글에 이미지 태그 포함.
- [x] **한국어 Validation 전수 보강**: `lang/ko/validation.php`에 누락된 exists/in/image/mimes/boolean/numeric/array 등 전체 규칙 추가. 필드명 한국어 매핑(title=제목, content=본문 등) 추가.
- [x] **StorePostRequest / UpdatePostRequest** `messages()` 메서드 추가 → 더 친화적인 한국어 안내("태그를 1개 이상 입력해 주세요" 등).
- [x] **NCP 썸네일 미리보기**: `Post/Edit.vue`에서 `post.card_image_url`을 `<img>` 소스로 이미 연결되어 있음 확인. 별도 수정 불필요.

---

## 📋 우선순위 요약

| 우선순위 | 항목 | 예상 시간 |
|---------|------|----------|
| 🔴 높음 | 포인트/티어 UI 헤더 반영 + 댓글 포인트 연동 | 1.5h |
| 🔴 높음 | 게시글 공유 기능 (링크 복사 + OG 태그) | 1.5h |
| 🟡 중간 | 관리자 공지 등록/노출 로직 검증 | 1h |
| 🟡 중간 | 관리자 게시글 편집 리치 에디터 도입 | 1.5h |
| 🟢 낮음 | 대시보드 차트 탭 전환 / 방문자 통계 점검 | 1h |
| 🟢 낮음 | SEO/Validation/NCP 마무리 점검 | 0.5h |

---

## ✅ 어제(4/14) 완료된 항목 (이월 없음 확인)

- [x] 북마크 토글 기능 (Backend + Frontend 연동 완료)
- [x] 포인트 Service 클래스 (`PointService.php`) 구현
- [x] 관리자 대시보드 통계 (DashboardController + Chart.js 기반 시각화)
- [x] 광고 레이아웃 기초 검토
