# 🚀 2026년 4월 8일 진행 목표 (Todo List)

지난 시간에 관리자 페이지에서 카테고리 시스템(DB 및 CRUD)과 회원 권한 관리를 완벽하게 세팅하고 마쳤습니다. 
오늘은 구축해둔 카테고리 데이터를 바탕으로 실제 회원들이 바라보는 사용자 화면(프론트엔드)을 연동할 차례입니다.

## 📌 오늘 해야 할 프론트엔드 카테고리 연동 작업

- [ ] **1. 게시글 메인(리스트) 화면 카테고리 필터링 적용**
  - 메인 화면 상단에 DB에서 불러온 동적 카테고리 탭(버튼) 렌더링.
  - 특정 카테고리 탭 클릭 시 해당 `category_id`에 속하는 게시글만 출력하도록 백엔드 라우트 및 컨트롤러(`PostController@index`) 필터 로직 구현.
  
- [ ] **2. 회원 게시글 작성/수정 폼 카테고리 선택 연동**
  - 유저 게시글 작성 화면(`Post/Create.vue`) 및 수정 폼에서 기존 하드코딩된 `type` 속성을 걷어내고, 실제 카테고리를 고를 수 있게 Select Box 형태로 변경.
  - 게시글 저장 서버측 유효성 검사(`StorePostRequest`, `PostController`)에 `category_id` 검증 추가.

- [ ] **3. 게시글 상세 페이지(`Show.vue`) 연결 데이터 갱신**
  - 게시글 디테일 페이지 화면 단에 기존 `type` 대신, 모델 관계도를 통해 불러온 진성 카테고리 명(`Category->name`)이 출력되도록 UI 문구 개선.
